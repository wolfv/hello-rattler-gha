[workspace]
name = "hello-rattler"
channels = ["conda-forge"]
platforms = ["linux-64", "osx-64", "osx-arm64", "win-64"]
preview = ["pixi-build"]

[package]
name = "hello-rattler"
version = "0.1.0"
description = "A simple Rust hello-world built with pixi-build-rust"

[package.build]
backend = { name = "pixi-build-rust", version = ">=0.1.0" }
